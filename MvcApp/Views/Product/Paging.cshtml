@model ProductListModel
 
<div class="page">
    <h2>Product List with Paging</h2>

    @if (Model.Products.Count == 0)
    {
        <h6>No Products yet.</h6>
    }
    else
    {
        @await Html.PartialAsync("_ProductList", Model)

        @await Component.InvokeAsync("Pager", Model.PagingInfo)
    }

</div>